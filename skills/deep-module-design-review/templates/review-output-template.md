# 深模块设计评审输出模板

> 只保留与任务有关的内容。事实、判断和假设必须分开表达。

## 1. 评审范围

- 目标模块：
- 读取文件：
- 未读取但可能相关：
- 是否允许改代码：
- 必须保持的行为：

## 2. 模块边界判断

- 模块提供的高层能力：
- 调用方当前责任：
- 边界结论：深模块 / 浅模块 / 混合
- 证据：
- 是否建议改变 public API：

## 3. Public API 深浅分析

| API | 高层意图 | 暴露的内部步骤 | 调用顺序约束 | 判断 |
|---|---|---|---|---|
|  |  |  |  |  |

复杂性是否被隐藏，还是被转移给调用方：

## 4. 内部主流程叙述性评估

- 核心 orchestrator / phase 主函数：
- 当前主流程是否高层叙述：
- 打断阅读流的因素：
- 同一层混杂的低层机制：
- lambda / helper 是否隐藏依赖：
- 是否存在复杂局部 lambda、巨型 inline block 或闭包链：
- 是否需要 `.cpp` internal helper：
- 是否需要 private phase helper：
- private phase helper 是否满足稳定领域动作、长期状态相关、副作用集中、调用点叙述性提升和替代方案更差：
- private/helper surface 是否服务可读性和状态副作用显式化：

## 5. Private Header Surface 审计

对 C++ / header-style 模块填写；其他语言可替换为 package/internal/private surface。

| private 类型或方法 | 长期状态/跨 phase 契约/phase-level 方法 | 主流程叙述性收益 | 是否只是单帧或单链临时细节 | 建议位置 | 判断 |
|---|---|---|---|---|---|
|  |  |  |  | header / cpp internal / 函数局部 / 删除 |  |

- private surface 是否暴露了完整执行脚本：
- 是否存在只能按固定顺序调用、参数高度重叠的 step helper：
- 应保留在 private header 的稳定概念：
- 应降级到 `.cpp`、函数局部或局部 lambda 的实现细节：
- private helper 是否满足稳定领域动作、长期状态相关、副作用集中、调用点叙述性提升和替代方案更差：

## 6. 主导可读性问题

1. 
2. 
3. 

不要仅以函数长度或类大小作为问题结论。

## 7. 复杂性来源

| 类别 | 当前表现 | 必要领域复杂性 | 可降低的结构复杂性 |
|---|---|---|---|
| 业务规则与编排 |  |  |  |
| 数据转换与计算 |  |  |  |
| 外部协议与 I/O |  |  |  |
| 策略变化 |  |  |  |
| 状态或资源管理（若适用） |  |  |  |
| 兼容与横切关注点 |  |  |  |

主要复杂性镜头：

## 8. 核心约束与模型

- 领域概念：
- 输入/输出契约：
- 变化轴：
- 调用方不应承担的知识：
- 错误或失败模型：
- 外部依赖：

仅对状态密集模块补充：

- state/identity owner：
- 生命周期与状态流：
- 关键不变量与副作用：

## 9. 应保留在主流程中的逻辑

```text
主流程
  -> 阶段一
  -> 阶段二
  -> 阶段三
```

保留原因：

### Phase-Level 与 Step-Level 接口判断

| 候选函数 | 完整领域动作或独立契约 | 是否只能与相邻步骤固定组合 | 独立复用/测试/失败边界 | 建议 |
|---|---|---|---|---|
|  |  |  |  | phase-level 保留 / 合并为更深接口 / 函数局部 |

- 推荐保留的 phase-level 接口：
- 建议收敛的 step helper：
- 可继续留在主流程中的直接代码：

## 10. 适合提取的内部机制

### Pure helper

- 

### Private helper

- 仅列出满足稳定领域动作、长期状态相关、副作用集中、调用点提升主流程叙述性，且替代方案更差的候选：

### 小结构体或结果对象

- 仅列出已通过“新增抽象必要性审计”的稳定领域输入、结果或错误契约：
- 中间语义默认先局部化；若建议提升为稳定类型，必须单独证明：

### 内部策略函数

- 

### 候选协作类或独立模块

| 候选类型 | 隐藏的复杂性 | 可见性建议 | 调用方收益 |
|---|---|---|---|
|  |  | private / internal / public |  |

## 11. 新增抽象必要性审计

轻量模式：若评审对象较小、没有新增抽象建议或用户只要求轻量判断，可直接填写：

```text
无新增类型建议 / 无需交接清单
```

无需机械展开本节全部表格。

先列出现有表示是否足够：

- 可直接复用的 domain object：
- 可复用的 map/set/vector 或其他容器：
- 可用清晰变量名表达的事实：
- 可用 type alias 表达的语义：
- 可降级为局部变量、局部 lambda 或函数局部 struct 的机制：

按以下类别给出结论：

### 建议保留或新增的深抽象

对每个候选填写：

```text
新增类型：
解决的问题：
新增的不变量：
为什么不能复用已有类型：
如果不用该类型，替代方案是什么：
收益是否大于新增阅读入口：
建议可见性：
```

仅用于缩短局部复杂类型名、且不跨函数边界传播的 type alias，可简化为一行：

```text
局部 type alias：原类型 -> alias；用途：缩短局部类型名；作用域：单函数；无新增语义或不变量。
```

### 建议降级为实现细节的抽象

| 抽象 | 当前可见性 | 建议位置 | 降级理由 |
|---|---|---|---|
|  |  | cpp internal / 函数局部 / 局部 lambda |  |

### 建议删除的浅包装

| 抽象 | 仅包装或搬运的内容 | 阅读成本 | 建议处置 | 可复用的已有表示 |
|---|---|---|---|---|
|  |  |  | 删除 / 局部变量 / type alias / 移入 cpp / 合并回调用函数 |  |

### 已有低收益抽象处置

| 已有抽象 | 未通过的正当性条件 | 是否主要依赖注释自证 | 优先处置 | 不应采取的补偿方式 |
|---|---|---|---|---|
|  |  | 是 / 否 | 删除 / 局部化 / type alias / 移入 cpp / 合并 | 补长注释 / 新增 wrapper / 新增解释层 |

### 不建议新增抽象的场景

| 场景 | 原拟新增类型 | 建议复用的表示 | 原因 |
|---|---|---|---|
|  |  |  |  |

审计结论必须同时覆盖拟新增抽象和已有抽象，并说明它们是否承载新不变量、稳定规则、独立生命周期、稳定复用或深模块边界。不能仅以“语义更清楚”或“已经存在”作为保留理由，也不能用更多注释补偿低收益抽象。

## 12. 实现前新增类型约束与交接清单

- 后续是否可能进入实现：是 / 否
- 当前方案能否在不新增类型的前提下实施：
- 设计约束：新增类型应先完成抽象必要性审计
- 本清单用途：作为后续实现任务的设计输入，不替代实现任务说明

轻量结论（适用于无新增类型建议或低风险小型任务）：

- 无新增类型建议 / 无需交接清单

存在跨函数、跨 phase、private header、生命周期、不变量或 API 风险时，再填写以下完整清单。

### 已通过审计的候选类型

| 类型 | 审计结论 | 新增不变量或深边界 | 为什么已有表示不足 | 最低必要可见性 | 状态 |
|---|---|---|---|---|---|
|  |  |  |  | 函数局部 / cpp internal / private / internal / public | 通过审计 |

### 拒绝或暂缓的候选类型

| 候选类型 | 结论 | 替代方案 | 重新评估条件 |
|---|---|---|---|
|  | rejected / deferred | 复用已有表示 / 局部变量 / lambda / type alias |  |

### 清单外类型需求的设计建议

若后续实现任务发现清单外类型需求，建议：

1. 补充反事实比较和必要性审计。
2. 比较复用已有表示的替代方案。
3. 更新本设计清单和实施方案。

本模板不规定实现代理如何暂停、修改或继续工作；具体实现行为由后续实现任务说明和项目治理决定。

本 skill 不直接修改业务代码，也不替代实现任务的范围、步骤、验证和交接要求。

## 13. 拆分建议与暂缓项

| 候选拆分 | 建议 | 原因 | 可见性或重新评估条件 |
|---|---|---|---|
|  | 拆分 / 保留 / 暂缓 |  |  |

重点检查职责内聚、调用方拼装成本、依赖方向、契约清晰度和新增阅读入口。状态模块再检查状态复制和不变量外泄。

## 14. 风险分级

### 低风险结构整理

- 改动：
- 保持条件：
- 最小验证：

### 中风险边界重组

- 改动：
- 潜在行为影响：
- 所需验证：

### 高风险行为或契约变更

- 改动：
- 改变的契约或策略：
- 是否应独立立项：

## 15. 分阶段重构计划

| 阶段 | 改动范围 | 必须保持 | 验证 | 停止条件 |
|---|---|---|---|---|
| 0 行为基线 |  |  |  |  |
| 1 约束与变化轴 |  |  |  |  |
| 2 纯函数/值对象 |  |  |  |  |
| 3 主流程或协作对象 |  |  |  |  |
| 4 内部边界调整 |  |  |  |  |
| 5 可选 public API 调整 |  |  |  |  |

## 16. 验证策略

- 单元测试：
- 契约测试：
- 状态序列测试（若适用）：
- 回归快照：
- 集成测试：
- 性能或运行验证：
- 暂无法验证：

## 17. 结论边界

- 已证实事实：
- 设计判断：
- 待验证假设：
- 未解决问题：
- public API 是否足够深：
- 内部 phase 主流程是否足够高层叙述：
- private/helper surface 是否服务可读性和状态副作用显式化：
