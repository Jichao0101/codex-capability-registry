# 深模块设计评审输出模板

> 只保留与任务有关的内容。事实、判断和假设必须分开表达。

## 1. 评审范围

- 目标模块：
- 读取文件：
- 未读取但可能相关：
- 是否允许改代码：
- 必须保持的行为：

## 2. 模块边界判断

- 模块提供的高层能力：
- 调用方当前责任：
- 边界结论：深模块 / 浅模块 / 混合
- 证据：
- 是否建议改变 public API：

### Public API

| API | 高层意图 | 暴露的内部步骤 | 调用顺序约束 | 判断 |
|---|---|---|---|---|
|  |  |  |  |  |

- 复杂性是被隐藏，还是被转移给调用方：

## 3. 内部主流程叙述性

- 核心 orchestrator / phase 主函数：
- 当前主流程是否高层叙述：
- 打断阅读流的因素：
- 同一层混杂的低层机制：
- 主流程叙述性失败模式：步骤化 helper / 临时集合外泄 / 隐式副作用 / 固定顺序脚本 / 低层算法混入 / 其他：
- lambda / helper 是否隐藏依赖：
- 是否需要 `.cpp` internal helper：
- 是否需要 private phase helper：
- private phase helper 是否满足稳定领域动作、长期状态相关、副作用集中、调用点叙述性提升和替代方案更差：

## 4. Private / Helper Surface 审计

| private/helper | 完整领域动作或 phase contract | 主要操作长期状态 | 参数来源风险 | 副作用显式 | 固定顺序 step | 建议位置 | 判断 |
|---|---|---|---|---|---|---|---|
|  | 是 / 否 | 是 / 否 | 长期状态 / 单帧事实 / 低层中间态 / 共享参数组 | 是 / 否 | 是 / 否 | header / cpp internal / 函数局部 / 合并 / 删除 |  |

- private/header surface 是否暴露完整执行脚本：
- 已有 helper 是否已先收敛，再考虑新增 helper：
- 应保留在 private/header 的稳定概念：
- 应降级到 `.cpp`、函数局部或局部 lambda 的实现细节：
- 应合并为更深 phase-level helper 的 step helper：
- 应删除的浅包装 helper：
- header 是否正在成为内部执行脚本；若是，最小收敛路径：

## 5. 参数来源审计

- 类长期状态：
- 单帧事实：
- 低层算法中间态：
- 多 helper 共享同批参数：
- 是否存在用成员化或 `Context` / `Payload` 打包掩盖参数来源的问题：
- 哪些参数具有跨 phase、跨帧、ownership、生命周期或不变量语义：

## 6. 主导可读性问题

1. 
2. 
3. 

不要仅以函数长度或类大小作为问题结论。

## 7. 复杂性来源与核心约束

| 类别 | 当前表现 | 必要领域复杂性 | 可降低的结构复杂性 |
|---|---|---|---|
| 业务规则与编排 |  |  |  |
| 数据转换与计算 |  |  |  |
| 外部协议与 I/O |  |  |  |
| 策略变化 |  |  |  |
| 状态或资源管理 |  |  |  |
| 兼容与横切关注点 |  |  |  |

- 主要复杂性镜头：
- 领域概念：
- 输入/输出契约：
- 变化轴：
- 调用方不应承担的知识：
- 错误或失败模型：
- 外部依赖：

仅对状态密集模块补充：

- state/identity owner：
- 生命周期与状态流：
- 关键不变量与副作用：

## 8. 主流程保留与拆分判断

```text
主流程
  -> 阶段一
  -> 阶段二
  -> 阶段三
```

- 保留原因：

| 候选函数 | 完整领域动作或独立契约 | 固定顺序组合 | 共享参数是否提示应合并 | 独立复用/测试/失败边界 | 建议 |
|---|---|---|---|---|---|
|  |  |  |  |  | phase-level 保留 / 合并 / cpp internal / 函数局部 / 删除 |

- 推荐保留的 phase-level 接口：
- 建议收敛的 step helper：
- 新增 helper 前已删除/降级/合并的已有 helper：
- 可继续留在主流程中的直接代码：

## 9. 适合提取、降级或暂缓的内部机制

### Pure / `.cpp` internal helper

- 

### Private phase helper

- 仅列出满足稳定领域动作、长期状态相关、副作用集中、调用点提升主流程叙述性，且替代方案更差的候选：

### 小结构体或结果对象

- 仅列出已通过新增抽象必要性审计的稳定领域输入、结果或错误契约：
- 中间语义默认先局部化；若提升为稳定类型，必须单独证明：

### 候选协作类或独立模块

| 候选类型 | 隐藏的复杂性 | 可见性建议 | 调用方收益 | 结论 |
|---|---|---|---|---|
|  |  | private / internal / public |  | 保留 / 新增 / 暂缓 |

## 10. 新增抽象必要性审计

若没有新增类型建议，可写：

```text
无新增类型建议 / 无需交接清单
```

先列出现有表示是否足够：

- 可直接复用的 domain object：
- 可复用的 map/set/vector 或其他容器：
- 可用清晰变量名表达的事实：
- 可用 type alias 表达的语义：
- 可降级为局部变量、局部 lambda 或函数局部 struct 的机制：

对每个建议新增或保留为稳定抽象的类型填写：

```text
新增类型：
解决的问题：
新增的不变量：
为什么不能复用已有类型：
如果不用该类型，替代方案是什么：
收益是否大于新增阅读入口：
建议可见性：
```

### 已有低收益抽象处置

| 抽象 | 未通过的条件 | 当前成本 | 优先处置 | 不应采取的补偿方式 |
|---|---|---|---|---|
|  |  |  | 删除 / 局部化 / type alias / 移入 cpp / 合并 | 补长注释 / 新 wrapper / 新解释层 |

### 拒绝或暂缓的候选类型

| 候选类型 | 结论 | 替代方案 | 重新评估条件 |
|---|---|---|---|
|  | rejected / deferred | 复用已有表示 / 局部变量 / lambda / type alias |  |

## 11. 风险分级

### 低风险结构整理

- 改动：
- 保持条件：
- 最小验证：

### 中风险边界重组

- 改动：
- 潜在行为影响：
- 所需验证：

### 高风险行为或契约变更

- 改动：
- 改变的契约或策略：
- 是否应独立立项：

## 12. 分阶段重构计划

| 阶段 | 改动范围 | 必须保持 | 验证 | 停止条件 |
|---|---|---|---|---|
| 0 行为基线 |  |  |  |  |
| 1 收敛已有 helper/private surface |  |  |  |  |
| 2 局部化低层机制 |  |  |  |  |
| 3 主流程或 phase helper |  |  |  |  |
| 4 内部边界调整 |  |  |  |  |
| 5 可选 public API 调整 |  |  |  |  |

## 13. 验证策略

- 单元测试：
- 契约测试：
- 状态序列测试（若适用）：
- 回归快照：
- 集成测试：
- 性能或运行验证：
- 暂无法验证：

## 14. 结论边界

- 已证实事实：
- 设计判断：
- 待验证假设：
- 未解决问题：
- public API 是否足够深：
- 内部 phase 主流程是否足够高层叙述：
- private/helper surface 是否服务可读性和状态副作用显式化：
